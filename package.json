{
  "name": "oauth",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:dev": "docker-compose -f docker-compose.dev.yml up",
    "start:prod": "docker-compose -f docker-compose.prod.yml up",
    "build": "docker-compose build --no-cache",
    "build:nodejs": "docker-compose build --no-cache nodejs",
    "build:api": "docker-compose build --no-cache api",
    "build:react": "docker-compose build --no-cache react",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "madge": "^7.0.0"
  }
}
